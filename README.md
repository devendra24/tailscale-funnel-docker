# 🚀 Tailscale Funnel + Docker Setup

This repo makes your app publicly available with HTTPS using Tailscale Funnel.

## Steps

1. Copy `.env.example` → `.env` and set values.
2. Start stack:
   ```bash
   docker compose up -d
