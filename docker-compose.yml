version: "3.9"

services:
  tailscale:
    image: tailscale/tailscale:stable
    hostname: ${HOSTNAME}
    volumes:
      - ./tailscale-data:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
      - ./setup.sh:/setup.sh
    cap_add:
      - NET_ADMIN
      - NET_RAW
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY}
      - HOSTNAME=${HOSTNAME}
      - APP_PORT=${APP_PORT}
      - DOMAIN=${DOMAIN}

    command: ["sh", "-c", "/setup.sh"]
    privileged: true
    networks:
      - internal

  nginx:
    image: nginx:alpine
    environment:
      - APP_PORT=${APP_PORT}
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - tailscale
    networks:
      - internal
      - proxy_net   # your future external network

  
  drone-server:
    image: drone/drone:2
    volumes:
    - ./drone-data:/data
    environment:
      - DRONE_DATABASE_DRIVER=sqlite3
      - DRONE_DATABASE_DATASOURCE=/data/database.sqlite
      - DRONE_GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - DRONE_GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - DRONE_RPC_SECRET=${DRONE_RPC_SECRET}
      - DRONE_SERVER_HOST=${DRONE_HOST}
      - DRONE_SERVER_PROTO=https
      - DRONE_LOGS_DEBUG=true
    networks:
      - proxy_net

  # drone-runner:
  #   image: drone/drone-runner-docker:1
  #   network_mode: "service:tailscale"
  #   depends_on:
  #     - drone-server
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #   environment:
  #     - DRONE_RPC_HOST=tailscale-node.chub-beta.ts.net
  #     - DRONE_RPC_PROTO=https
  #     - DRONE_RPC_SECRET=${DRONE_RPC_SECRET}
  #     - DRONE_RUNNER_CAPACITY=3
  #     - DRONE_RUNNER_NAME=tailscale-runner
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: "2.0"
  #         memory: 2g
  
networks:
  internal:
    driver: bridge
  proxy_net:
    driver: bridge